# 昆仑驰枢 (Kunlun ChiShu) —— 成品油智能补货与供应链协同平台

## 📖 一、项目概述

**昆仑驰枢**是一套面向成品油零售行业的智能供应链管理系统，旨在为加油站、油库等场景提供从**需求预测 → 安全库存计算 → 自动补货 → 订单管理 → 调度配送**全链路的数字化决策支持。

系统名称取自"昆仑"（中国石油精神象征）与"驰枢"（快速运转的中枢），寓意打造成品油供应链的智慧中枢，实现精准补货、高效运转。

> **版本**: v1.0  
> **定位**: MVP（最小可行产品），已完成核心MRP引擎与数据看板的开发，调度模块与设备管理模块为预留扩展。

---

## 🛠 二、技术栈

| 层级 | 技术选型 | 版本 |
|------|----------|------|
| **后端框架** | Spring Boot | 3.5.14 |
| **Java版本** | Java | 17 |
| **ORM框架** | MyBatis-Plus | 3.5.9 |
| **数据库** | MySQL | 8.0.33 |
| **工具库** | Hutool | 5.8.35 |
| **简化代码** | Lombok | — |
| **前端框架** | Vue 3 + TypeScript | — |
| **构建工具** | Vite (rolldown) | — |
| **UI组件库** | Element Plus | — |
| **图表可视化** | ECharts 5 | — |
| **地图服务** | 高德地图 (AMap) | — |
| **状态管理** | Pinia | — |
| **路由** | Vue Router 4 | — |

---

## 🏗 三、项目结构

```
kunlun-chishu/
├── kunlun-backend/                    # 后端 Spring Boot 服务
│   ├── src/main/java/com/kunlun/kunlun_backend/
│   │   ├── KunlunBackendApplication.java       # 应用入口
│   │   ├── algorithm/                 # 核心算法层
│   │   │   ├── forecast/             # 预测算法
│   │   │   │   ├── ExponentialSmoothing.java   # 指数平滑(一次/二次)
│   │   │   │   ├── LinearRegression.java      # 线性回归
│   │   │   │   └── MovingAverage.java         # 移动平均
│   │   │   ├── inventory/            # 库存算法
│   │   │   │   ├── SafetyStockCalculator.java # 安全库存计算
│   │   │   │   └── EOQCalculator.java        # 经济订货批量
│   │   │   └── metrics/              # 评估指标
│   │   │       └── MetricsCalculator.java     # MSE/MAE/MAPE/TS
│   │   ├── config/                   # 配置层
│   │   │   ├── CorsConfig.java      # 跨域配置
│   │   │   ├── MybatisPlusConfig.java        # MyBatis-Plus分页配置
│   │   │   ├── MyMetaObjectHandler.java      # 自动填充时间戳
│   │   │   ├── DatabaseConnectionChecker.java # 数据库连接检查
│   │   │   └── GlobalExceptionHandler.java   # 全局异常处理
│   │   ├── controller/               # 控制器层 (REST API)
│   │   │   ├── BiController.java    # BI看板接口
│   │   │   ├── MrpController.java   # MRP智能补货引擎接口
│   │   │   ├── OrderController.java # 订单管理接口
│   │   │   ├── PredictionController.java    # 预测日志接口
│   │   │   ├── DispatchController.java      # 调度管理接口(预留)
│   │   │   ├── EquipmentController.java     # 设备管理接口(预留)
│   │   │   ├── StationController.java       # 站点管理接口
│   │   │   ├── MaterialController.java      # 油品管理接口
│   │   │   └── UserController.java          # 用户管理接口
│   │   ├── dto/                      # 数据传输对象
│   │   │   ├── MrpRequest.java      # MRP运行请求
│   │   │   ├── MrpResult.java       # MRP运行结果
│   │   │   ├── StationStatusVO.java # 站点库存状态
│   │   │   ├── SalesTrendVO.java    # 销量趋势
│   │   │   ├── DataHealthVO.java    # 数据健康度
│   │   │   ├── ForecastAccuracyVO.java     # 预测准确率
│   │   │   └── InventoryCompareVO.java     # 库存对比
│   │   ├── entity/                   # 数据实体层
│   │   │   ├── Station.java         # 加油站/油库
│   │   │   ├── Material.java        # 油品信息(92#/95#/98#/0#/-10#)
│   │   │   ├── SalesHistory.java    # 销量历史(核心数据表)
│   │   │   ├── StationInventory.java # 站点库存(含乐观锁)
│   │   │   ├── PlannedOrder.java    # 补货订单
│   │   │   ├── PredictionLog.java   # 预测日志(黑匣子)
│   │   │   ├── DispatchRoute.java   # 调度路线(预留)
│   │   │   ├── Equipment.java       # 设备台账(预留)
│   │   │   ├── OilPrice.java        # 油价历史
│   │   │   ├── WeatherData.java     # 天气数据
│   │   │   └── User.java            # 系统用户
│   │   ├── mapper/                   # MyBatis数据访问层
│   │   ├── service/                  # 业务服务层
│   │   │   ├── mrp/
│   │   │   │   ├── MrpService.java          # MRP核心引擎
│   │   │   │   └── DataCleaningService.java # 数据清洗服务
│   │   │   ├── bi/
│   │   │   │   └── BiService.java           # BI看板聚合服务
│   │   │   └── order/
│   │   │       └── OrderService.java        # 订单管理服务
│   │   └── common/
│   │       └── Result.java          # 统一API返回格式
│   └── pom.xml                       # Maven依赖配置
│
└── kunlun-frontend/                   # 前端 Vue 3 应用
    ├── src/
    │   ├── main.ts                   # 应用入口
    │   ├── App.vue                   # 根组件
    │   ├── api/                      # API接口层
    │   │   ├── base.ts              # 基础CRUD封装
    │   │   ├── bi.ts                # BI看板API
    │   │   ├── mrp.ts               # MRP补货API
    │   │   └── order.ts             # 订单管理API
    │   ├── router/
    │   │   └── index.ts             # 路由配置(5模块+登录)
    │   ├── store/                    # Pinia状态管理
    │   │   ├── app.ts               # 应用全局状态
    │   │   ├── auth.ts              # 认证状态
    │   │   ├── mrp.ts               # MRP状态
    │   │   └── bi.ts                # BI状态
    │   ├── layout/
    │   │   ├── DefaultLayout.vue    # 默认布局(侧边栏+顶栏)
    │   │   ├── SideMenu.vue         # 侧边导航菜单
    │   │   └── TopHeader.vue        # 顶部栏
    │   ├── pages/
    │   │   ├── Login.vue            # 登录页
    │   │   ├── dashboard/
    │   │   │   ├── Map.vue          # 库存健康地图
    │   │   │   ├── Quality.vue      # 数据质量监控
    │   │   │   └── SalesTrend.vue   # 销量趋势分析
    │   │   ├── replenishment/
    │   │   │   ├── MrpRun.vue       # 运行MRP补货
    │   │   │   ├── Suggestion.vue   # 补货建议列表
    │   │   │   └── PredictionLog.vue # 预测日志
    │   │   ├── order/
    │   │   │   ├── Pending.vue      # 待处理工单
    │   │   │   └── History.vue      # 历史订单
    │   │   ├── dispatch/
    │   │   │   ├── VehicleDispatch.vue  # 车辆调度
    │   │   │   └── EquipmentLedger.vue  # 设备台账
    │   │   └── basic/
    │   │       ├── Station.vue      # 站点管理
    │   │       ├── Material.vue     # 油品管理
    │   │       └── User.vue         # 用户管理
    │   ├── components/               # 通用组件
    │   │   ├── StatCard.vue         # 统计卡片
    │   │   ├── StatusTag.vue        # 状态标签
    │   │   ├── MapContainer.vue     # 地图容器
    │   │   └── DrawerForm.vue       # 抽屉表单
    │   └── utils/
    │       └── request.ts           # Axios请求封装
    ├── index.html
    └── package.json
```

---

## 🧠 四、核心功能模块

### 4.1 BI 智能看板 (`/dashboard`)

系统首页仪表盘，提供全景数据概览：

- **库存健康地图** (`Map.vue`)
  - 高德地图可视化，以红/黄/绿标记展示所有站点的库存健康状态
  - 顶部统计卡片：运行组合数、库存健康数、预警数、告警数
  - 库存对比柱状图（当前库存 vs 安全库存）
  - 待执行订单列表（最近5条）
  - 底部数据表格，展示全部站点×油品库存明细

- **数据质量监控** (`Quality.vue`)
  - 数据完整率：有记录天数 / 应有天数
  - 数据准确率（正常值占比）
  - 异常数据计数
  - 最新数据日期

- **销量趋势分析** (`SalesTrend.vue`)
  - 展示指定站点最近N天的销量趋势折线图
  - 按油品分类展示

> 库存状态判定规则：当前库存 ≥ 2×安全库存 → 🟢 SAFE | ≥ 1×安全库存 → 🟡 WARNING | < 安全库存 → 🔴 DANGER

---

### 4.2 MRP 智能补货引擎 (`/replenishment`)

系统核心，实现**5步法**自动补货计算流程：

```
┌──────────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  ① 数据清洗  │ →  │ ② 需求预测   │ →  │③ 安全库存计算│ →  │④ 净需求计算  │ →  │⑤ 生成补货订单│
│ 前向填充+    │    │ 指数平滑     │    │ 工业级公式   │    │ Net = F - S  │    │ 取整到千升   │
│ 3σ异常检测   │    │ α自动寻优    │    │ Is = h×√(...)│    │ - O + SS + D │    │ 写入订单表   │
└──────────────┘    └──────────────┘    └──────────────┘    └──────────────┘    └──────────────┘
```

#### ① 数据清洗 (DataCleaningService)
- **缺失值处理**: 生成连续日期序列，缺失日期使用前向填充（Forward Fill），用前一天值补齐
- **异常值检测**: 3σ 原则自动标记异常数据点
- **数据过滤**: 可配置是否排除异常值参与后续计算

#### ② 需求预测 (ExponentialSmoothing)
- **一次指数平滑**: St = α·Yt + (1-α)·St₋₁
- **二次指数平滑**: 在一次平滑基础上再做平滑，适合带趋势的数据
- **α自动寻优**: 0.01~0.99，步长0.01遍历，选 MSE 最小的参数
- **模型自动选择**: 同时运行一次和二次平滑，选 MSE 更小的模型输出
- 同时提供 **线性回归**（趋势外推）和 **移动平均** 作为基准对比算法

#### ③ 安全库存计算 (SafetyStockCalculator)
采用**完整工业级公式**（参考马开良、韩玉琴 2024《车用能源储运销技术》）：

```
Is = h × √(T × σo² + Oa² × σT²)
```

- `h`: 安全系数（按服务水平查表：95%→1.645, 99%→2.326, 99.9%→3.09）
- `T`: 平均到货期（天）
- `σo`: 日出库量标准差
- `Oa`: 日均出库量
- `σT`: 到货期标准差

> 该公式同时考虑**销量波动**(σo)和**到货期波动**(σT)，比简化版公式 `SS = Z × σ × √L` 更贴合成品油行业实际。

同时提供 **经济订货批量(EOQ)** 计算器：`EOQ = √(2DS/H)`

#### ④ 净需求计算
```
净需求 = 预测需求 - 当前库存 - 在途库存 + 安全库存 + 不可动库存(库底油)
```

若净需求 > 0，进入下一步生成补货订单。

#### ⑤ 自动生成补货订单
- 补货量取整到**千升**（成品油行业惯例）
- 写入 `planned_order` 表，记录完整决策链路（预测方法、参数、MSE）
- 写入 `prediction_log` 表，记录预测日志用于后续模型评估

---

### 4.3 预测日志与模型监控

`prediction_log` 表作为系统**黑匣子**，记录每次预测的完整信息：

- 模型名称、参数（α值等）
- 预测值与实际值（可后续回填比对）
- 误差指标：MSE（均方误差）、MAE（平均绝对误差）、MAPE（平均绝对百分比误差）
- **跟踪信号 (Tracking Signal)**: 当 `|TS| > 4` 时建议重新校准模型

---

### 4.4 订单管理 (`/order`)

| 功能 | 说明 |
|------|------|
| **待处理工单** | 分页查看PENDING/CONFIRMED状态订单 |
| **确认订单** | PENDING → CONFIRMED，同时增加站点**在途库存** |
| **完成订单** | CONFIRMED → COMPLETED，扣减在途库存并**到货入库** |
| **取消订单** | PENDING → CANCELLED |
| **历史订单** | 查询COMPLETED/CANCELLED等终态订单记录 |

订单状态流转图：
```
PENDING ──确认──→ CONFIRMED ──完成──→ COMPLETED
   │                                   
   └──取消──→ CANCELLED
```

---

### 4.5 调度管理 (`/dispatch`，预留扩展)

- **车辆调度**: 当前返回模拟数据，未来计划实现基于**节约算法**或**遗传算法**的路径优化
- **设备台账**: 油库和加油站设备信息管理，参考数字孪生体描述

---

### 4.6 基础数据管理 (`/basic`)

| 模块 | 功能 |
|------|------|
| **站点管理** | 加油站/油库的CRUD、按名称/区域筛选。依据GB50156-2021国标设计 |
| **油品管理** | 支持92#/95#/98#汽油、0#/-10#柴油的物理属性管理（密度、到货期、单价等） |
| **用户管理** | 系统用户的CRUD，4种角色：调度员、运营经理、系统管理员、站长 |

---

## 📊 五、数据库设计概要

| 表名 | 说明 | 核心字段 |
|------|------|----------|
| `station` | 站点信息 | 编码、名称、区域、经纬度、服务水平、默认到货期 |
| `material` | 油品信息 | 编码、名称(92#等)、密度、类型、到货期、单价 |
| `sales_history` | 销量历史 | 站点ID、油品ID、日期、销量、是否异常 |
| `station_inventory` | 站点库存 | 当前库存、不可动库存(库底油)、在途库存、安全库存、罐容、乐观锁version |
| `planned_order` | 补货订单 | 站点ID、油品ID、建议补货量、需求日期、状态、预测方法/参数 |
| `prediction_log` | 预测日志 | 站点ID、油品ID、预测日期、模型、参数、MSE/MAE/MAPE/TS |
| `dispatch_route` | 调度路线(预留) | 起终点、距离、预估时间、车型、运价 |
| `equipment` | 设备台账(预留) | 站点ID、设备名称/类型/型号、安装/维保日期 |
| `oil_price` | 油价历史 | 油品ID、调价日期、单价、调整幅度 |
| `weather_data` | 天气数据 | 区域、日期、天气类型、温度、降雨量、节假日因子 |
| `user` | 系统用户 | 用户名、密码、角色、所属站点 |

---

## 🔗 六、API 接口一览

### BI看板 `/api/bi`
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/bi/station-status` | 所有站点库存状态（含经纬度，用于地图） |
| GET | `/api/bi/station/{id}/recent-sales` | 某站点近N天销量趋势 |
| GET | `/api/bi/data-health` | 数据健康度（完整性/及时性/异常数） |
| GET | `/api/bi/forecast-accuracy` | 各模型预测准确率对比 |
| GET | `/api/bi/inventory-compare` | 各站点库存对比数据 |

### MRP补货 `/api/mrp`
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/mrp/run` | 运行MRP补货计算，输入站点ID+油品ID+预测天数 |

### 预测日志 `/api/prediction`
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/prediction/logs` | 分页查询预测执行记录 |

### 订单管理 `/api/orders`
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/orders` | 分页查询（支持status筛选） |
| PUT | `/api/orders/{id}/confirm` | 确认订单（更新在途库存） |
| PUT | `/api/orders/{id}/complete` | 完成订单（到货入库） |
| PUT | `/api/orders/{id}/cancel` | 取消订单 |

### 基础数据管理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST/PUT/DELETE | `/api/stations` | 站点CRUD |
| GET/POST/PUT/DELETE | `/api/materials` | 油品CRUD |
| GET/POST/PUT/DELETE | `/api/users` | 用户CRUD |

### 预留接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/dispatch/routes` | 调度路线查询（暂返回模拟数据） |
| GET | `/api/equipment/list` | 设备台账查询（暂返回模拟数据） |

---

## 🎯 七、设计亮点

### 7.1 工业级算法落地
- 完整实现**考虑双重波动**的安全库存公式（销量波动 + 到货期波动），区别于简化版 `SS = Z × σ × √L`
- α参数**自动寻优**，避免人工调参的主观性与不稳定性
- 一次/二次平滑**自动对比选优**，系统自行选择最适合数据的模型

### 7.2 数据质量保障
- **前向填充**缺失日期，保证数据连续性
- **3σ异常检测**自动识别异常销售数据并标记
- 预测日志作为**黑匣子**，记录完整的决策链路与误差指标
- 跟踪信号实时监控，`|TS| > 4` 触发模型重校准建议

### 7.3 工程化实践
- 统一API返回格式 `Result<T>`（code=200成功）
- 全局异常处理器
- MyBatis-Plus自动填充（创建时间/更新时间）
- 库存表使用**乐观锁**（`@Version`），防止高并发更新冲突
- 订单操作全流程 `@Transactional` 事务保护
- 前向填充的数据标记 `source="前向填充"`，可追溯数据来源

### 7.4 可扩展架构
- 算法层独立封装，新增预测模型只需实现统一接口
- 调度管理/设备管理模块预留完整的数据表结构
- 前后端分离架构，RESTful API设计

---

## 🚀 八、快速开始

### 环境要求
- **JDK** 17+
- **Maven** 3.6+
- **MySQL** 8.0+
- **Node.js** 18+

### 后端启动
```bash
# 1. 克隆项目
git clone <your-github-repo-url>
cd kunlun-chishu/kunlun-backend

# 2. 修改数据库配置 (application.yml)
# 配置MySQL连接信息

# 3. 初始化数据库（执行SQL脚本导入表结构和初始数据）

# 4. 编译启动
./mvnw spring-boot:run
```

### 前端启动
```bash
cd kunlun-chishu/kunlun-frontend

# 1. 安装依赖
npm install

# 2. 启动开发服务器
npm run dev
```

### 访问地址
- 前端页面: `http://localhost:3000`
- 后端API: `http://localhost:8080`

---

## 📋 九、版本规划

| 版本 | 内容 |
|------|------|
| **v1.0**（当前） | ✅ MRP核心引擎（5步法）、BI看板（地图+图表）、订单管理、基础数据CRUD |
| **v1.1**（规划中） | 🔲 多特征预测模型（融合天气/节假日/油价因子）、ARIMA模型 |
| **v1.2**（规划中） | 🔲 调度路径优化（节约算法/遗传算法）、实时追踪 |
| **v2.0**（愿景） | 🔲 AI大模型集成（智能分析报告）、数字孪生3D可视化 |

---

## 📚 十、参考资料

本项目在算法设计和行业知识方面参考了以下文献和标准：

- 盛志刚 (2014). 《成品油销售企业库存管理研究》. 云南大学MBA论文
- 赵振学等 (2022). 《成品油供应链优化模型》. 油气储运
- 马开良、韩玉琴 (2024). 《合理库存模型在成品油供应链中的应用实践》. 车用能源储运销技术
- GB 50156-2021 《汽车加油加气加氢站技术标准》

---

## 📄 开源协议

本项目遵循 MIT License。

---

> **昆仑驰枢** — 以数据驱动成品油供应链的每一次精准决策。
